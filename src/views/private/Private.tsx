import React, { ReactElement, } from 'react';

import { textDanger, } from 'privateViewsStyle/private.scss';

const Private: React.SFC = (): ReactElement => <div className={textDanger}>Private</div>;

export default Private;
